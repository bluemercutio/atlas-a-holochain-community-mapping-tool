{
  "name": "tests",
  "private": true,
  "scripts": {
    "test": "foreach -g \"**/*.test.ts\" -x \"node --loader ts-node/esm #{path}\" --no-C"
  },
  "dependencies": {
    "@msgpack/msgpack": "^2.7.0",
    "@holochain/client": "^0.12.0",
    "@holochain/tryorama": "^0.11.0",
    "@goenning/foreach-cli": "^1.9.0",
    "tape": "^5.6.1",
    "tape-promise": "^4.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  },
  "type": "module"
}
